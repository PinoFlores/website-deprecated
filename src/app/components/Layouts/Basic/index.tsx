import React, {FunctionComponent} from "react";
import {BasicLayoutProps} from "./types";

const BasicLayout: FunctionComponent<BasicLayoutProps> = (props) => {
  return <React.Fragment>sss</React.Fragment>;
};

export default BasicLayout;
